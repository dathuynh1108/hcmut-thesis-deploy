version: '3'
services:
  haproxy:
    image: haproxy:alpine
    network_mode: host
    volumes:
      - "./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg"
    extra_hosts:
      - "hcmut-thesis-gateway:192.168.49.2"